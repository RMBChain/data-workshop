---
description: 开发/排查时如何查看后端 API 容器日志（与 Desiign_3_Api §13 一致）
alwaysApply: true
---

# 查看 API 日志

后端在 Docker 中运行时，可用以下命令跟踪 API 容器标准输出（含 uvicorn 与业务日志）：

```bash
docker logs -f workshop-api-dev
```

若容器名因 compose 配置不同（例如 `workshop-api`），先用 `docker ps` 确认服务名后再替换上述名称。

# 重启api
```bash
docker restart workshop-api-dev
```
