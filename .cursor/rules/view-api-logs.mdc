---
description: 开发/排查时如何查看后端 API 容器日志（与 Design_3_Api §15 一致）
alwaysApply: true
---

# 查看 API 日志

后端在 Docker 中运行时，可用以下命令跟踪 API 容器标准输出（含 uvicorn 与业务日志）：

```bash
docker logs -f data-workshop-api
```

`devops/docker-compose.yml` 中 API 服务的 Compose 名为 **`data-workshop-api`**，`container_name` 亦为 **`data-workshop-api`**。若本地自行 `docker run` 起了别的名字，先用 `docker ps` 确认后再替换。

# 重启api
```bash
docker restart data-workshop-api
```
