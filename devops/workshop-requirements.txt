# 供 devops/Dockerfile.workshop 使用；须与 backend/requirements.txt 保持一致
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart
pyyaml
httpx
pydantic-settings>=2.0
bitsandbytes>=0.43.0
psutil>=5.9
# torch.cuda 会 import pynvml；使用 NVIDIA 官方 nvidia-ml-py，避免旧 PyPI 包 pynvml 的 FutureWarning
nvidia-ml-py>=12.560.0
