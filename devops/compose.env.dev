# 供「后端容器开发」与 Readme §开发端口 8701/8702 一致时使用：
#   docker compose --env-file devops/compose.env.dev -f docker-compose.yml -f devops/docker-compose.dev.yml up -d --build api
# run-backend-dev.ps1 已带此文件
WORKSHOP_HOST_API_PORT=8702
WORKSHOP_HOST_WEB_PORT=8701
