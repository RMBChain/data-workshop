# 数据工坊（Data Workshop）— UI 设计说明

> 依据 `Readme_1_requirement.md` 整理；界面文案与标签**仅使用中文**；技术栈：Vue3、Ant Design Vue、Vite、TypeScript、ECharts。

---

## 1. 设计目标与原则

| 目标 | 说明 |
|------|------|
| 减少认知负担 | 主流程按「数据 → 数据集 → 训练 → 试跑/评测」顺排，次要能力（合并、导出、系统信息）副栏或顶栏可达。 |
| 与约束一致 | 全路径强调 **CPU 跑训练/推理**；若展示 GPU 探测信息，需附**不参与本工具主路径**的说明（见 §8）。 |
| 可观测性 | 训练与评测页突出日志、曲线、资源占用；长任务有队列与状态机反馈。 |
| 轻量不简陋 | 空状态、加载、失败、取消均有明确文案与可恢复操作。 |

**视觉基线**（与 Ant Design Vue 一致即可）：主色建议偏工具型蓝/靛，侧栏深灰；圆角、间距遵循 AD 默认 token，不额外做重型皮肤。

---

## 2. 整体布局

```
┌──────────────────────────────────────────────────────────────┐
│  [Logo] 数据工坊          [环境：开发/生产]    [系统信息] [用户/帮助]  │
├──────────┬───────────────────────────────────────────────────┤
│ 主导航    │  主内容区（PageHeader + 面包屑 + 卡片/表格/表单）      │
│          │                                                    │
│ · 数据导入│                                                    │
│ · 数据集  │                                                    │
│ · 训练    │                                                    │
│ · 推理沙盒│                                                    │
│ · 合并    │                                                    │
│ · 评测    │                                                    │
│ · 导出    │  （或「数据导出」与数据集/训练页内联入口，二选一见 §2.1）  │
└──────────┴───────────────────────────────────────────────────┘
```

- **顶栏右侧「系统信息」**：跳转 §8 抽屉或独立页。  
- **底栏**（可选）：仅显示当前工作区根路径与版本号，不占主视觉。

### 2.1 导航项合并建议

- **「导出」**：若与训练产物强相关，可放在 **训练** 子标签「产物 / 导出」+ **合并** 页成功后的快捷导出；独立「导出」导航保留时，内页汇总 HF 兼容权重与说明即可。  
- **MVP 交付**优先保证：导入、数据集、训练、推理沙盒；合并、评测、导出可同级靠后但路径清晰。

---

## 3. 页面与组件规格

### 3.1 数据导入

**目的**：从 Label Studio 经 API 拉取项目；展示导入后的元数据与可解析图片路径（无本地上传、无「商城 URL 入图」入口）。

| 区域 | 组件/行为 |
|------|-----------|
| 连接配置 | 表单：LS 基址（可提示 `WORKSHOP_LABEL_STUDIO_URL` 与宿主机/容器内差异）、Token；「测试连接」按钮，成功/失败用 Alert。 |
| 项目选择 | 下拉或表格：项目 ID/名称；「拉取并导入」主按钮。 |
| 结果预览 | 表格列：任务 ID、图片路径/缩略图（若有 URL 则仅展示链接类型说明）、是否已解析。 |
| 说明区 | 折叠面板：SFT 字段示例与需求文档中 JSON/JSONL 样例的只读展示（与 §1.2 一致）。 |

**空状态**：未连接 LS 时引导用户查看根目录 README 的 Docker 起 LS 方式。

---

### 3.2 数据集（构建与划分）

**目的**：将导入数据按模板转为 SFT；**qwen-vl** 对话模板、自动补 `<image>`；按比例划分训练/验证/测试；**版本**快照在 `{workspace}/versions/`。

| 子区块 | 内容 |
|--------|------|
| 源数据 | 选择已导入的 LS 项目/任务范围；只读显示样本条数。 |
| 构建选项 | 模板与模型族固定提示「与训练默认 Qwen3-VL 族一致 / qwen-vl 模板」；单选/开关：是否自动补 `<image>`。 |
| 划分 | 三档比例输入或滑块（训练/验证/测试，三者之和 100%）；**随机种子**可选数字输入。 |
| 版本 | 表格：时间、备注、路径标识；操作：回滚、导出；**新建处理**前强制备注或可跳过（产品可选）。 |
| 输出预览 | 小窗口 JSON 片段预览 1 条，确认字段形态与 `Readme_1` 中示例一致。 |

**交互**：长耗时操作用 Modal 进度 + 可取消；完成后 Toast + 跳「训练」或留在此页点「去训练」。

---

### 3.3 训练

**目的**：SFT/LoRA 参数表单、YAML 导入导出、任务队列、日志、曲线、资源监控。

| 子区块 | 内容 |
|--------|------|
| 基础模型 | 二选一：**ModelScope 模型名/ID** 与 **本机已下载路径**；表格展示参数量/内存提示（2B 约 10GB LoRA 等，与需求表一致）。 |
| 超参数 | 表单 + 部分滑块：LoRA rank（8/16/32）、alpha、target modules（`q_proj,v_proj` 可编辑）、学习率、batch（1–4）、梯度累积 4、epochs 3、图片分辨率 448/512、max pixels 256×256 等。 |
| YAML | 「从 YAML 导入」「导出 YAML」；导出含注释示例项更佳。 |
| 提交 | 「提交训练」→ 进入队列；页面内或子路由 **任务列表**（见下）。 |
| 任务监控 | 表格：队列状态（**等待 → 运行 → 成功/失败**）；操作：取消、删除、重试。行展开或侧抽屉：**日志**（见下「训练日志区」）。 |
| 曲线 | ECharts 内嵌：**训练/验证 loss**、**学习率**；与当前选中任务联动；无数据时折线区占位。 |
| 资源 | 小卡片区：**CPU、内存**；轮询如 2s 刷新，附「本任务为 CPU 执行」短说明。 |

#### 训练日志区（滚动与跟随）

| 要求 | 说明 |
|------|------|
| 可见滚动 | 日志展示在 **固定高度、纵向滚动** 的等宽区域（`pre` / 虚拟列表均可），训练进行中内容持续增长时，用户能 **上下滚动** 查看任意历史片段。 |
| 默认跟随最新 | 当用户停留在 **底部附近**（实现可定阈值，例如距底部不足约 80px）时，每次日志更新后 **自动滚到底部**，形成「尾部不断追加」的观感。 |
| 暂停跟随 | 用户 **向上滚动** 离开底部区域后，**不再强制滚底**，避免打断阅读；显示 **「跟随最新」** 或 **「回到底部」** 按钮，一键恢复滚底并继续跟随。 |
| 刷新节奏 | MVP 可用 **轮询** `GET .../logs`（如 1～2s）；后续可换 **SSE**（`Desiign_3_Api.md` §5），前端同一套滚底逻辑复用。 |
| 高亮与复制 | **ERROR / WARNING** 行关键词高亮（颜色区分即可）；提供 **复制全部日志**。极长日志时可先 **纯文本滚动** 保证流畅，高亮可用虚拟列表或分段渲染逐步增强。 |
| 任务结束 | 任务 **成功/失败/取消** 后停止轮询或关闭 SSE；保留最终日志，仍可按上述规则滚动查看。 |

**顶栏提醒**：`Alert` 类型 info：训练与推理均在 **纯 CPU** 上执行，速度受本机 CPU/内存影响。

---

### 3.4 推理沙盒

**目的**：对话式 UI；加载**本机训练输出**的模型/适配器；**切换模型不重启**；图片来源仅 **已导入的 LS 项目数据** 或同链路上下文。

| 区域 | 行为 |
|------|------|
| 模型条 | 下拉：已注册 checkpoint / LoRA 合并结果路径；**切换即切模型**（无需整页刷新，状态提示「已切换」）。 |
| 图像上下文 | 从「当前 LS 项目可关联的条目/列表」选图，**无本地上传、无 URL 贴图**（防误解可加简要禁用说明）。 |
| 对话区 | 类聊天布局：用户消息、助手消息、**图片缩略**；输入框多行。 |
| 工具条 | 保存对话、历史恢复、清空、**导出为 Markdown/JSON**。 |

**空状态**：未选模型时主区引导先去训练或合并。

---

### 3.5 LoRA 合并

| 表单项 | 说明 |
|--------|------|
| 基础模型路径 | 输入或本机选择器。 |
| LoRA 路径 | 支持**多个**，列表增删。 |
| 输出路径 | 输入校验非空。 |
| 主操作 | 「执行合并」→ 调用 `swift merge-lora` 同类流程（进度 + 日志）。 |
| 完成后 | 成功：「校验并加载」+ **跳转推理沙盒试跑**；失败：日志与重试。 |

---

### 3.6 评测

| 区域 | 内容 |
|------|------|
| 数据来源 | 与 §1.1 一致：经 LS/导入管线的 **JSONL 或问答对** 选择。 |
| 指标开关 | 准确率、BLEU、ROUGE 等可勾选。 |
| 结果 | 汇总数字 + **逐条结果列表**（表格，可搜/分页）；**简单图表**（柱状/趋势按指标类型定）。 |
| 输出 | 导出报告；**历史记录**存工作区侧（列表 + 时间 + 源数据版本）。 |

---

### 3.7 导出（若独立页）

- 汇总 **HF 兼容浮点/合并后权重** 路径；附说明：与 ms-swift 可加载/可再训为准；**MVP 不引导** 依赖 **CUDA 的 bitsandbytes 4-bit**（与纯 CPU 范围一致，避免误导）。

---

### 3.8 系统信息

| 展示项 | 注意 |
|--------|------|
| Python、PyTorch 等 | 版本与能力（CPU/是否有 CUDA 等客观信息）。 |
| GPU 信息 | 若显示：旁注 **仅环境探测，不参与本工具内训练/推理**（与需求一致）。 |
| 路径 | 模型缓存 `~/.cache/modelscope`；数据 `./data`；输出 `./output`；可一键复制。 |

使用 **Drawer** 或独立页，避免打断主工作流。

---

## 4. 全局模式与状态

- **任务长时间运行**：非阻塞；用户可离开页面，自任务中心再进入。  
- **训练日志**：见 §3.3「训练日志区」— 等宽字体、可滚动、默认跟随尾部；**WebSocket / SSE** 为可选实现，轮询亦可，滚底与「暂停跟随」行为一致。  
- **错误**：后端错误信息中文化映射；`Result` 不可用时至少展示 HTTP 状态与 trace id（若有）。  
- **仅中文**：菜单、按钮、表头、空状态、错误、Tooltip 均不使用英文主导文案。

---

## 5. 与需求条目的映射（便于验收）

| 需求章节 | 对应 UI |
|----------|---------|
| §1 数据导入 | 3.1 |
| §2 数据集 | 3.2 |
| §3 训练 | 3.3 |
| §4 推理沙盒 | 3.4 |
| §5 合并 | 3.5 |
| §6 导出 | 3.7 + 训练产物入口 |
| §7 评测 | 3.6 |
| §8 系统信息 | 3.8 |
| 技术栈 | 前文本头；图表统一 ECharts。 |

---

## 6. 附：推荐路由草图（实现参考）

| 路径（示例） | 页面 |
|--------------|------|
| `/` 或 `/import` | 数据导入 |
| `/datasets` | 数据集 |
| `/train` | 训练（可含子路由 `jobs/:id`） |
| `/playground` | 推理沙盒 |
| `/merge` | LoRA 合并 |
| `/eval` | 评测 |
| `/export` | 导出（可选） |
| `/system` 或系统抽屉 | 系统信息 |

以上为界面结构设计文档，实现时可随 Ant Design Vue 4.x 的 Layout/Menu/Form/Table/Modal 等直接落地。
